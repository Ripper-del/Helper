[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = ["pip install -r bot/requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python bot/bot.py"
